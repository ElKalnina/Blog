<p>
  <strong>Title:</strong>
  <%= @tasks.title %>
</p>
<p>
  <strong>Text:</strong>
  <%= @tasks.text %>
</p>

<h2>Comments</h2>
<%= render @tasks.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_task_path(@tasks) %> |
<%= link_to 'Back', tasks_path %>
